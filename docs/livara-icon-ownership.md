
## Stage 3 — Align Noctalia icon documentation (2026-09-01)

The support repository documentation referred to the retired `lambda-thick.svg` as the Noctalia launcher asset, although the active bar button is owned and rendered by `noctalia-conf` as `widget.control-center`. The note now points to the versioned Japanese Kanji SVG and keeps the Fastfetch cat asset description separate. No shell adapter or runtime feature was changed.
